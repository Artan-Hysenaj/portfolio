import { faIcons } from '@fortawesome/free-solid-svg-icons';
import { StatsItemProps } from './StatsItem';

const base: StatsItemProps = {
  title: 'Hello World',
  number: 1,
  icon: faIcons,
};

export const mockStatsItemProps = {
  base,
};
