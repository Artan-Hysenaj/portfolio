import { specializations } from '@/dummy-data/specializations';
import { SpecializationsProps } from './Specializations';

const base: SpecializationsProps = {
  specializations,
};

export const mockSpecializationsProps = {
  base,
};
