import { progresses } from '@/dummy-data/progresses';
import { ProgressBarsProps } from './ProgressBars';

const base: ProgressBarsProps = {
  data: progresses,
};

export const mockProgressBarsProps = {
  base,
};
