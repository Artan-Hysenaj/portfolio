import { SecondaryLayoutProps } from './SecondaryLayout';

const base: SecondaryLayoutProps = {
  children: '{{component}}',
  title: '{{title}}',
};

export const mockSecondaryLayoutProps = {
  base,
};
